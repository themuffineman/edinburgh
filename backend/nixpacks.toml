[phases.setup]
run = "python3 -m venv venv"

[phases.build]
run = "venv/bin/pip install -r requirements.txt && playwright install --with-deps chromium"

[phases.start]
run = "venv/bin/uvicorn main:app --host 0.0.0.0 --port ${PORT}"

